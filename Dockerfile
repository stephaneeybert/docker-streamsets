FROM streamsets/datacollector

#RUN sudo addgroup -g 1001 stephane
#RUN sudo adduser -u 1000 -G stephane -H --shell /bin/false -D stephane
#USER stephane

# TODO Not used for now. Could be used when having a complete properties file.
#COPY sdc.properties /etc/sdc/sdc.properties
